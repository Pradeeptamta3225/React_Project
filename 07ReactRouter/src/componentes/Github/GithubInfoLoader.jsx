
// import { useLoaderData } from "react-router-dom";

// // This is the loader function to fetch data from GitHub API
// export const GithubInfoLoader = async () => {
//   const response = await fetch('https://api.github.com/users/pradeeptamta3225');
//   const data = await response.json();  // Parse the JSON from the response
//   return data;  // Return the parsed data
// };

// // Component to display the fetched data
// const GithubInfoComponent = () => {
//   const data = useLoaderData();  // Use the loader data in the component

//   return (
//     <div>
//       <h1>{data.login}</h1>
//       <p>{data.bio}</p>
//       {/* Render other data as needed */}
//     </div>
//   );
// };

// export default GithubInfoComponent;
